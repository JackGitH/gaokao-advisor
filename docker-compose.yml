version: '3.8'
services:
  gaokao:
    build: .
    container_name: gaokao-advisor
    ports:
      - "8001:8001"
    restart: always
    environment:
      - DATABASE_PATH=/app/data/gaokao.db
    volumes:
      - gaokao-data:/app/data

volumes:
  gaokao-data:
